import { Option } from './options-parser';
/**
 * @internal
 * @param options Options to build the parseCmdArgs function for either OData or OpenApi.
 */
export declare function parseCmdArgsBuilder<GeneratorOptionsT>(options: {
    cliOptions: Record<string, Option<any>>;
    commandText: string;
}): (argv: string[]) => GeneratorOptionsT & {
    config?: string;
};
